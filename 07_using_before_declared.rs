fn main() {
    // println!("{}", x); // ❌ Cannot use before declared
    let x = 10; // ✅ Declare first
    println!("x is {}", x);
}

// Must declare any variable, You cannot just use it anywhere